private enum MoltinAPIEndpoints {
    /// sourcery: model = Product, path = "/path"
    case product
    /// sourcery: model = Category, path = "/category"
    case category
    /// sourcery: model = Brand, path = "/brand"
    case brand
}
